{
  "name": "pokegen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "rm -rf docs && mv dist docs",
    "deploy": "npm run build && git add docs && git commit -m 'Deploy to GitHub Pages' && git push origin master",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "chance": "^1.1.11",
    "hacktimer": "^1.1.3",
    "inferno": "^8.2.3",
    "localforage": "^1.10.0",
    "vite-plugin-inferno": "^0.0.1"
  }
}